NAME
	arrayp - test if a variable is an array

SYNOPSIS
	int arrayp(mixed arg)

DESCRIPTION
	Check if 'arg' is an array.

RETURN VALUE
	Returns 1 if 'arg' is an array, 0 otherwise.

SEE ALSO
	floatp, functionp, intp, stringp, objectp, mappingp, nilp

