NAME
	owner_file - determine the owner of a file

SYNOPSIS
	string owner_file(string file)

DESCRIPTION
	Determine the owner of a file. The file doesn't have to exist,
	if it doesn't then the would be owner is returned.

RETURN VALUE
	Returns a string with the name of the owner of a file.

EXAMPLES
	string name;

	name = owner_file("/tmp/sirdude.txt");
	write("The owner of that file is: " + name + "\n");

SEE ALSO
	afun/owner_object, afun/file_name
