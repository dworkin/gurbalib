NAME
	trim_str - remove extra whitespace

SYNOPSIS
	string trim_str(string str);

DESCRIPTION
	Replaces all whitespace with a single blank space, it also
	removes trailing and leading whitespace.

RETURN VALUE
	A new string.  See description for details.

EXAMPLES
	string x;
	x = trim_str(" X      Woo Hoo      X ");
	write("(" + x + ")\n");   /* Should print " (X WooHoo X) " */

SEE ALSO
	afun/add_comma, afun/empty_str, afun/fmt_str, afun/lowercase,
	afun/pad_string_left, afun/replace_string, afun/rexplode, afun/strcmp,
	afun/strstr, afun/str2val, kfun/explode, kfun/implode,
	afun/parse_range, kfun/parse_string, kfun/sscanf,
	afun/uppercase
