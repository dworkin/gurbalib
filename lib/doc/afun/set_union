NAME
	set_union - Return the union of two sets.

SYNOPSIS
	mixed *set_invert(mixed *a, mixed *b, varargs int r);

DESCRIPTION
	Get the union of sets 'a' and 'b'.  The optional argument removes
	repeated elements from the union.

RETURN VALUE
	The union, of the two sets, optionally with repeats removed.

SEE ALSO
	afun/bubblesort, afun/set_difference, afun/set_intersection,
	afun/set_invert, afun/set_remove_repeats, afun/set_symmetric_difference

