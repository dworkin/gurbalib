NAME
	functionp - test if a variable is a pointer to a function

SYNOPSIS
	int functionp(mixed arg)

DESCRIPTION
	Check if 'arg' is a function.

RETURN VALUE
	Returns 1 if 'arg' is a function, 0 otherwise.

EXAMPLES
	mixed myfun;

	if (!functionp(myfun)) {
	   write("Error that is not a function.\n");
	}

SEE ALSO
	afun/arrayp, afun/dump_value, afun/floatp, safun/fmt_time,
	afun/intp, afun/lowercase, afun/mappingp, afun/nilp, afun/objectp,
	afun/stringp, afun/uppercase
