NAME
	random_element - returns an element from an array randomly

SYNOPSIS
	mixed random_element(mixed *arr)

DESCRIPTION
	Returns random element from the given array.  Returns nil if 
	array is empty.

RETURN VALUE
	Any single element from given array.
EXAMPLES
	object *peoples;
	object winner;

	peoples = query_users();
	winner = random_element(peoples);
	write("The winner is " + winner->query_Name() + "\n");

SEE ALSO
	afun/filter_array, afun/map_array, afun/member_array, afun/member_map,
	afun/split_array, afun/uniq_array

