NAME
	make_dir - Create a directory.
		
SYNOPSIS
	static int make_dir(string dir)

DESCRIPTION
	Create the directory specified by dir.
		
RETURN VALUE
	Returns 1 on success and 0 on failure.

SEE ALSO
	cat_file, copy, editor, get_dir, read_file, rename_file, 
	remove_dir, remove_file, restore_object, save_object, valid,
	valid_read, valid_write, write_file

