NAME
	replace_string - replace instances of a substring

SYNOPSIS
	string replace_string(string str, string old, string new)

DESCRIPTION
	In str, replace instances of the substring old with new.  
	I.E. replace_string("old old", "old", "new") = "new new".

RETURN VALUE
	Returns the modified string.

EXAMPLES
	string stuff;

	stuff = "Hello big badddd wolf.";
	stuff = replace_string(stuff,"badddd","bad");
	write("Stuff = " + stuff + "\n");

SEE ALSO
	afun/empty_str, afun/fmt_str, afun/pad_string_left, afun/rexplode,
	afun/strcmp, afun/strstr, afun/str2val, afun/trim_str, kfun/explode,
	kfun/implode, kfun/parse_string, kfun/sscanf


