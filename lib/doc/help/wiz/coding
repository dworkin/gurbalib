This mud uses a language called LPC its an object oriented language
similar to C.  

Everything in the mud is an object.  The main base objects are:
	OBJECT, MONSTER, ROOM, and PLAYER

objects you create will inierit one of these base classes or another
subclass.  Then you'll set various properties and or add functions
to the objects to make them function properly.


