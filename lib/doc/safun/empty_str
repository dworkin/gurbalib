NAME
	empty_str - determine empty string

SYNOPSIS
	int empty_str(string str);

DESCRIPTION
	Checks for !str, nilp(str), and str == "".

RETURN VALUE
	Returns 1 if any of the conditions are met, else 0.

EXAMPLES
	string str;

	if (empty_str(str)) {
	   write("Error str undefined.\n");
	}

SEE ALSO
	safun/add_comma, safun/fmt_str, afun/lowercase,
	safun/pad_string_left, safun/replace_string, safun/rexplode,
	safun/strcmp, safun/strstr, safun/str2val, kfun/explode, kfun/implode,
	safun/parse_range, kfun/parse_string, kfun/sscanf, safun/trim_str,
	afun/uppercase
