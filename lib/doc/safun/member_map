NAME
	member_map - see if an element is a key or value in a map

SYNOPSIS
	int member_map(mixed item, mapping map)

DESCRIPTION
	Checks if 'item' is a key or value in the mapping 'map'.

RETURN VALUE
	Returns 1 if 'item' is either a key or a value in 'map' and
	0 otherwise.

EXAMPLES
	mapping x;

	if (member_map("5",x)) {
		write("5 is in mapping x.\n");
		return 1;
	}

SEE ALSO
	afun/call_array, afun/filter_array, afun/filter_mapping,
	afun/map_array, safun/member_array, safun/random_element,
	safun/split_array, safun/uniq_array
