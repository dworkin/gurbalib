NAME
	cat_file - message entire contents of a file to this_player()
		
SYNOPSIS
	int cat_file(string filepath)

DESCRIPTION
	Message the entire contents of a file to this_player().

RETURN VALUE
	Returns 1 for success and 0 for failure.

EXAMPLES
	int x;

	if (!cat_file("/tmp/sirdude.txt")) {
	   write("Error\n");
	}

NOTES
	This afun only exists for code outside /kernel.

SEE ALSO
	afun/copy, afun/editor, afun/get_dir, afun/make_dir, afun/read_file,
	afun/rename_file, afun/remove_dir, afun/remove_file, afun/require_priv,
	afun/restore_object, afun/save_object, afun/unguarded, afun/valid,
	afun/valid_read, afun/valid_write, afun/write_file
