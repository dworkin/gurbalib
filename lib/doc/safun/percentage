NAME
	percentage -

SYNOPSIS
	int percentage(mixed part, mixed total)

DESCRIPTION
	Take part/total and convert it to a number 0-100.

RETURN VALUE
	int

EXAMPLES
	int x, y;

	x = 5, y=100;
	write("x = " + x + "\n");
	write("y = " + y + "\n");
	write(" x/y = " + percentage(x, y) + "%\n");

SEE ALSO
	 safun/round, safun/round_to_next, kfun/ceil, kfun/floor, kfun/fmod
