inherit OBJECT;

void setup(void) {
   set_id("rock");
   set_adj("large");
   set_long("A pretty boooooring grey rock.");
   set_gettable(1);
   set_weight(10);
   /* This isn't a special rock... */
   set_value(0);
}
