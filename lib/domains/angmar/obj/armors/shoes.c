inherit "/std/armor";

void setup(void) {
   set_id("shoes");
   add_ids("pair of shoes");
   set_short("A pair of shoes");
   set_long("The tattered shoes are not useful for keeping your feet dry when walking through water.");
   set_gettable(1);

   set_ac(1);

   set_slot("feet");
   set_value(20);
   set_size(2);
   set_weight(1);
}
