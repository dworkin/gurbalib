inherit "/std/armor";

void setup(void) {
   set_id("cloak");
   set_short("A cloak made of twigs and leaves");
   set_long("It once belonged to the troll-king of Darkwood.");
   set_gettable(1);
   set_slot("body");
   set_ac(3);
   set_value(100);
   set_size(3);
   set_weight(5);
}
