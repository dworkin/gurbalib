inherit "/std/armor";

void setup(void) {
   set_id("cloak");
   set_short("A cloak made of twigs and leaves");
   set_long("It once belonged to the troll-king of Darkwood.");
   set_gettable(1);

   set_ac(1);

   set_slot("body");
   set_value(20);
   set_size(2);
   set_weight(1);
}
