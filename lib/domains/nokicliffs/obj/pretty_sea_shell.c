inherit "/std/object";

void setup(void) {
   set_id("sea shell");
   set_adj("pretty");
   set_short("A pretty sea shell");
   set_long("A pretty sea shell. It must be useful for something.");
   set_gettable(1);
   set_weight(1);
   set_value(1);
}
